# M0 milestone — smoke evidence log

Run: scratch anchor instance (dsh **0.1.2-rc.1**, the version chamber runs / npm
`next`) on 127.0.0.1:32131, `DSH_HOME=.smoke/m0-home`, driver
`scripts/smoke/{instance,m0}.mjs`. Full raw run: `docs/milestones/M0-raw.log`
(verbatim `m0-full.log` excerpts below; probe scripts under `.smoke/`).

## ① Third-party client module loads in the instance (install + inventory + boot graph)

User-install command, run for real against a fresh profile:

```
$ dsh plugin --profile web add file:.smoke/dsh-mcp-scope-0.1.0.tgz
Packages are cloned from the content-addressable store to the virtual store.
Progress: resolved 96, reused 96, downloaded 0, added 97, done
Done in 640ms using pnpm v11.21.0
bundles after add: @deepseek-ai/dsh-base, @deepseek-ai/dsh-web-app, dsh-mcp-scope
```

After restart, plugin inventory (row-level projection of live loader entries):

```
row: {"entryId":"include:mcp-scope","moduleName":"dsh-mcp-scope","enabled":true,"fiberPhase":"active"}
```

Browser half of the dual-face package is discovered by `dsh-client-modules` and
served in the web GUI boot graph (verified across 3+ consecutive clean boots —
note: the FIRST boot right after `dsh plugin add` may race the module scan; a
restart makes it deterministic, which is the documented install step):

```
entry url: /plugins/??dsh-mcp-scope/client.js&rev=3cffceb8801eccc7-45
fetch: 200 bytes 51828
head: window.__ModuleLoader__.load({ id: "dsh-mcp-scope", factory: (require) => {
has settings.section reg: true
```

`settings/describe` after install — namespace registered with document schema and
resolved defaults:

```
mcp-scope ns: revision=0 value={"servers":[],"overrides":{}}
```

## ② Settings domain: third-party namespace read/write + revision fence

- `settings/mutate` `{ns:'mcp-scope', ops:[{op:'set', path:['servers'], value:[…fixture stdio def…]}]}` → ok,
  namespace revision 0 → 1, document stored in `$DSH_HOME/settings.yaml` under `mcp-scope:`.
- Stale write (expectedRevision 0 after revision moved to 1) refused host-side:

```
rpc settings/mutate failed: settings/conflict: settings namespace "mcp-scope" changed since it was read
 (expected revision 0, now 1)  {details:{ns,expected:0,actual:1}}
settings-conflict observed: true
```

## ⑤ Credentials: write-only over the wire, resolved into the MCP child env

```
credentials/describe {refs:[MCP_SCOPE_TEST_TOKEN]} → {"MCP_SCOPE_TEST_TOKEN":{"configured":true,"source":"file","writable":true}}
value never rides the wire: true
```

Fixture MCP server marker (the stdio child writes lifecycle events to a marker
file; `tokenPresent` proves the credential env landed):

```
{"ts":1788623541811,"event":"start","tokenPresent":true}
{"ts":1788623541812,"event":"connected","tokenPresent":true}
```

The marker also proves **host-half lifecycle on boot**: with the server definition
persisted in the namespace, each instance boot spawns the stdio MCP server and
connects (config survives restarts; plugin row stays `active` through all
mutations/session activity).

## ③⑥ Workspace gate + install evidence

Two workspaces created (`ws-a`, `ws-b`); override `overrides[ws-b][fixture]=true`
written via one path op; sessions created in both (session ids recorded in
`M0-raw.log`); config flips applied against live sessions without error. The
injection gate itself (enabled-only registration into agent tool scopes, revoke on
disable) is proven by the host test-suite mounting the REAL ToolRuntime + real
`dsh-scope` agent contexts over two on-disk workspaces — see `tests/host/agents.spec.ts`
and `docs/host-notes.md` (there is no HTTP surface that lists a session's model
tools; recon `runtime-test-env.md` §5/§8).

## Suite (in-repo evidence)

`vitest run` → **87 tests / 11 files green**, incl. tools naming contract (17),
real-stdio supervisor tests (crash→reconnect, list_changed re-sync, budget
exhaustion), settings namespace with the REAL settings-file provider (hooks,
validate refusal, revisions), transport env scrub + real child env round-trip,
and the agent-scope gating suite. Typecheck (src + tests) clean; full build emits
`lib/index.js` + `lib/client.js` + `lib/types`.

## Remaining gaps (carried to M1)

- Live capture of a *model request's* `tools[]` array (mock-LLM capture) was
  originally unreachable headlessly; it was later closed by driving the rc.1
  remote mux (`session/follow`) to activate sessions headlessly — see
  `M1.md` and `M1-live-capture.log` (R3 PASS: on-workspace turn carries
  `mcp__fixture__*`, off-workspace turn does not).
- Render evidence for the section page in a real browser (jsdom render tests +
  bundle-in-boot-graph stand in; see `tests/client/section-render.spec.tsx`).
